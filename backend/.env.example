# ============================================================
# Configurações do Backend - Portal COGESPA
# Copie este arquivo para .env e ajuste os valores
# ============================================================

# Chave secreta do Flask (MUDE ISSO!)
SECRET_KEY=cogespa-secret-key-MUDE-ISSO-2026

# Chave secreta do JWT (MUDE ISSO!)
JWT_SECRET=jwt-cogespa-secret-MUDE-ISSO

# Domínios permitidos para CORS (separados por vírgula)
# Coloque aqui o domínio do seu frontend na Vercel
CORS_ORIGINS=http://localhost:3000,http://localhost:5500,https://portal-cogespa.vercel.app

# Usuário admin padrão (usado apenas no seed inicial)
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASSWORD=cogespa2026
